so 
int n = k; //even if it came from input 
int arr[n];

both are VLA variable length array so use const int n instead of int n;


if arr[9999]={1,2,3}
then lefted elemets will be zero or \0 (as null char in case or char)

error if you do cout<<arr[10000]

`sizeof()` har baar use nahi karte kyunki function me array pass karne par array pointer ban jata hai.
Isliye `sizeof(arr)` actual array size nahi batata, sirf pointer size batata hai.
Isi wajah se array size alag variable (`n`) me pass kiya jata hai while traversing loops.
